# run the code

python gapp*.py  

it will open the web for recording an audio or do the following

# prerequisites

```
pip install gradio 
pip install langchain
pip install langchain-community
pip install oci 
pip install stopwatch.py
```
